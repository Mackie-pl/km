<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
  <!-- AndroidTV support -->
  <uses-feature android:name="android.software.leanback" android:required="false" />

  <application
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:theme="@style/Theme.km_test"
    android:usesCleartextTraffic="${usesCleartextTraffic}">
    <activity
      android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
      android:launchMode="singleTask"
      android:label="@string/main_activity_title"
      android:name=".MainActivity"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
        <!-- AndroidTV support -->
        <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
      </intent-filter>

      <!-- Offer this app in "Open with" for Markdown files whose source app
           reports a proper markdown MIME type (e.g. Google Files, Solid Explorer). -->
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="content" />
        <data android:scheme="file" />
        <data android:mimeType="text/markdown" />
        <data android:mimeType="text/x-markdown" />
      </intent-filter>

      <!-- Fallback for ".md" files shared with a generic MIME type: stock
           Android has no built-in extension->MIME mapping for ".md", so most
           file managers send "application/octet-stream" or similar instead of
           "text/markdown". Match on the file extension directly in that case. -->
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data
          android:scheme="content"
          android:host="*"
          android:mimeType="*/*"
          android:pathPattern=".*\\.md" />
        <data
          android:scheme="file"
          android:host="*"
          android:mimeType="*/*"
          android:pathPattern=".*\\.md" />
      </intent-filter>

      <!-- Google Drive OAuth redirect. Google returns the authorization code to
           this reversed-domain custom scheme (the Android client's reversed
           client_id); the OS routes it back here as a deep link, captured by
           tauri-plugin-deep-link. MUST equal GOOGLE_OAUTH_ANDROID_SCHEME in
           src/app/core/adapters/cloud/gdrive/config.ts. -->
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="com.googleusercontent.apps.311145204704-q253fucp3v6fnintq3c285kkvhrqqg5c" />
      </intent-filter>
    </activity>

    <provider
      android:name="androidx.core.content.FileProvider"
      android:authorities="${applicationId}.fileprovider"
      android:exported="false"
      android:grantUriPermissions="true">
      <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/file_paths" />
    </provider>
  </application>
    <!-- ANDROID FS PLUGIN. AUTO-GENERATED. DO NOT REMOVE. -->
    
    <!-- ANDROID FS PLUGIN. AUTO-GENERATED. DO NOT REMOVE. -->
</manifest>